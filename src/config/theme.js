import { DefaultTheme } from "react-native-paper";
const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#76A6ED",
    accent: "#fff",
  },
};
export default theme;
